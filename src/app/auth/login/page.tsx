const LoginPage = async () => {
  return (
    <>
      {/* <Suspense fallback={<div>Loading login form...</div>}>
        <LoginForm setUi={() => {}} />
      </Suspense> */}
    </>
  )
}

export default LoginPage
